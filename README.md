# Encryption-Decryption-Program.py
A Python based Encryption and Decryption tool using a substitution cipher with randomized key generation. This program securely transforms plaintext into encrypted text and allows reverse decryption using the same key mapping.
